"""
Test suite for video processing package.
"""

from pathlib import Path
import sys

# Add the parent directory to Python path to allow imports of the main package
sys.path.insert(0, str(Path(__file__).parent.parent.absolute()))
